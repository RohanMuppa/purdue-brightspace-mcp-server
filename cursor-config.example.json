{
  "_comment": "Add this snippet to your Cursor config: .cursor/mcp.json in project root (project-level) or ~/.cursor/mcp.json (global)",
  "_instructions": [
    "Replace /ABSOLUTE/PATH/TO/ with the actual path to your cloned brightspace-mcp-server repo",
    "The 'env' object is intentionally empty — no credentials belong in this file",
    "Run 'brightspace-auth' first to authenticate before using with Cursor",
    "After adding this config, restart Cursor to load the MCP server"
  ],
  "mcpServers": {
    "brightspace": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/brightspace-mcp-server/build/index.js"],
      "env": {}
    }
  }
}
