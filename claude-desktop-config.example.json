{
  "_comment": "Add this snippet to your Claude Desktop config file at ~/Library/Application Support/Claude/claude_desktop_config.json",
  "_instructions": [
    "Replace /ABSOLUTE/PATH/TO/ with the actual path to your cloned purdue-brightspace-mcp repo",
    "The 'env' object is intentionally empty — no credentials belong in this file",
    "Run 'purdue-brightspace-auth' first to authenticate before using with Claude Desktop",
    "After adding this config, restart Claude Desktop to load the MCP server"
  ],
  "mcpServers": {
    "purdue-brightspace": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/purdue-brightspace-mcp/build/index.js"],
      "env": {}
    }
  }
}
